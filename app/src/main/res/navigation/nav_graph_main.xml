<?xml version="1.0" encoding="utf-8"?>
<navigation
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph_bottom"
    app:startDestination="@id/menuSync"
    >

    <fragment
        android:id="@+id/menuSync"
        android:name="net.dimterex.sync_client.ui.folder.sync.SyncFragment"
        app:menu="@menu/sync_fragment_menu"
        android:label="@string/sync_title" />

    <fragment
        android:id="@+id/menuSettings"
        android:name="net.dimterex.sync_client.ui.folder.settings.SettingsFragment"
        app:menu="@menu/settings_fragment_menu"
        android:label="@string/settings_title" />

</navigation>